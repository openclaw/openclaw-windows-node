<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClaw.SetupEngine.UI.Pages.ProgressPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Disabled">

    <Grid RowDefinitions="Auto,*">

        <!-- Header -->
        <StackPanel Grid.Row="0" Padding="56,14,56,0" Spacing="4">
            <Image Source="ms-appx:///OpenClaw.SetupEngine.UI/Assets/Setup/Lobster.png" Width="44" Height="44"
                   HorizontalAlignment="Center" Margin="0,0,0,2" />
            <TextBlock Text="Setting up your gateway"
                       Style="{StaticResource TitleTextBlockStyle}"
                       HorizontalAlignment="Center" />
            <TextBlock x:Name="SubtitleText"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center" />
        </StackPanel>

        <!-- Steps + live activity flow together in one scroll region so the
             ledger opens downward naturally instead of being pinned to the bottom. -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Padding="56,12,56,16" Spacing="16" VerticalAlignment="Top">

                <!-- Step rows -->
                <StackPanel x:Name="StepsPanel" Spacing="4" />

                <!-- Live activity ledger -->
                <Expander IsExpanded="True"
                          HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                          Header="Live activity">
                    <Grid RowDefinitions="*,Auto">
                        <Border Grid.Row="0" CornerRadius="4" Height="128" Padding="12,8"
                                Background="#0E0F12">
                            <ScrollViewer x:Name="LogScroller"
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Auto">
                                <TextBlock x:Name="LogText"
                                           FontFamily="Cascadia Mono, Consolas"
                                           FontSize="11"
                                           Foreground="#C3CAD4"
                                           TextWrapping="NoWrap"
                                           IsTextSelectionEnabled="True" />
                            </ScrollViewer>
                        </Border>
                        <Button x:Name="OpenLogButton" Grid.Row="1"
                                Content="Open log file" FontSize="12"
                                HorizontalAlignment="Right" Margin="0,6,0,0"
                                Click="OpenLog_Click" Padding="10,4" />
                    </Grid>
                </Expander>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</Page>
