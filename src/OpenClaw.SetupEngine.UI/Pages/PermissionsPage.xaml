<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClaw.SetupEngine.UI.Pages.PermissionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Disabled">

    <Grid RowDefinitions="Auto,*,Auto" Padding="48,28,48,24">

        <StackPanel Grid.Row="0" Spacing="8" HorizontalAlignment="Center">
            <Image Source="ms-appx:///OpenClaw.SetupEngine.UI/Assets/Setup/OpenClawMascot.png"
                   Width="44" Height="44" HorizontalAlignment="Center"
                   AutomationProperties.AccessibilityView="Raw" />
            <TextBlock Text="Grant permissions"
                       Style="{StaticResource TitleTextBlockStyle}"
                       HorizontalAlignment="Center" />
            <TextBlock TextWrapping="Wrap" TextAlignment="Center" MaxWidth="480"
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Text="OpenClaw works best when it can send notifications, access your camera and microphone, capture your screen, and know your location. Grant permissions below." />
            <HyperlinkButton x:Name="RefreshButton" Content="Refresh status"
                             HorizontalAlignment="Center" Click="Refresh_Click" />
        </StackPanel>

        <StackPanel x:Name="PermRows" Grid.Row="1" Spacing="8" Margin="0,24,0,0"
                    VerticalAlignment="Top" />

        <Grid Grid.Row="2" ColumnDefinitions="*,Auto">
            <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8"
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        IsTabStop="False" AutomationProperties.Name="Step 5 of 6">
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="20" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupIndicatorAccentBrush}" />
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
            </StackPanel>
            <Button x:Name="BackButton" Grid.Column="0" Content="Back"
                    MinHeight="44" MinWidth="96" HorizontalAlignment="Left"
                    Click="BackToWizard_Click" />
            <Button x:Name="NextButton" Grid.Column="1" Content="Next"
                    MinHeight="44" MinWidth="140" HorizontalAlignment="Right"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="Next_Click" />
        </Grid>

    </Grid>
</Page>
