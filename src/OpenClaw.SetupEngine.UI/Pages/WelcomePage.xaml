<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClaw.SetupEngine.UI.Pages.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Disabled">

    <Grid Padding="48,28,48,24" RowDefinitions="Auto,*,Auto">

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Spacing="12">
            <Image x:Name="MascotHero"
                   Source="ms-appx:///OpenClaw.SetupEngine.UI/Assets/Setup/OpenClawMascot.png"
                   Width="96" Height="96" HorizontalAlignment="Center"
                   AutomationProperties.AccessibilityView="Raw" />
            <TextBlock Text="Set up OpenClaw"
                       Style="{StaticResource TitleTextBlockStyle}"
                       HorizontalAlignment="Center" />
            <TextBlock TextWrapping="Wrap" TextAlignment="Center" MaxWidth="460"
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Text="OpenClaw runs your agent through a gateway. Choose how to set it up on this PC — you can change this later." />
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Top" Margin="0,24,0,0" Spacing="12"
                    MaxWidth="560" HorizontalAlignment="Stretch">

            <Button x:Name="WslButton" Click="WslButton_Click"
                    HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                    Padding="16" CornerRadius="4"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource AccentFillColorDefaultBrush}"
                    BorderThickness="1"
                    AutomationProperties.Name="Install in WSL, recommended safest isolation">
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="16">
                    <Border Grid.Column="0" Width="44" Height="44" CornerRadius="6"
                            Background="{ThemeResource AccentFillColorDefaultBrush}" VerticalAlignment="Top">
                        <FontIcon Glyph="&#xE756;" FontSize="20" IsTextScaleFactorEnabled="False"
                                  Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                    </Border>
                    <StackPanel Grid.Column="1" Spacing="3">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock x:Name="WslInstallTitle"
                                       Text="Install in WSL"
                                       Style="{StaticResource BodyStrongTextBlockStyle}"
                                       VerticalAlignment="Center" />
                            <Border CornerRadius="4" Padding="7,1" VerticalAlignment="Center"
                                    BorderBrush="{ThemeResource AccentTextFillColorPrimaryBrush}" BorderThickness="1">
                                <TextBlock Text="Recommended"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                            </Border>
                        </StackPanel>
                        <TextBlock TextWrapping="Wrap"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Text="Recommended for the safest local boundary. Uses an isolated Ubuntu WSL 2 instance. Requires virtualization and WSL 2." />
                    </StackPanel>
                    <FontIcon Grid.Column="2" Glyph="&#xE76C;" FontSize="14" IsTextScaleFactorEnabled="False"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                </Grid>
            </Button>

            <Button x:Name="InstallButton" Click="StartButton_Click"
                    HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                    Padding="16" CornerRadius="4"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    AutomationProperties.Name="Install on Windows natively">
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="16">
                    <Border Grid.Column="0" Width="44" Height="44" CornerRadius="6"
                            Background="{ThemeResource SubtleFillColorSecondaryBrush}" VerticalAlignment="Top">
                        <FontIcon Glyph="&#xE756;" FontSize="20" IsTextScaleFactorEnabled="False"
                                  Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                    </Border>
                    <StackPanel Grid.Column="1" Spacing="3">
                        <TextBlock x:Name="InstallTitle"
                                   Text="Install on Windows (native)"
                                   Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock TextWrapping="Wrap"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Text="Installs OpenClaw directly on Windows with managed startup. Simpler setup, with a weaker isolation boundary." />
                    </StackPanel>
                    <FontIcon Grid.Column="2" Glyph="&#xE76C;" FontSize="14" IsTextScaleFactorEnabled="False"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                </Grid>
            </Button>

            <Button x:Name="ConnectButton" Click="AdvancedSetup_Click"
                    HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                    Padding="16" CornerRadius="4"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    AutomationProperties.Name="Connect to an existing gateway">
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="16">
                    <Border Grid.Column="0" Width="44" Height="44" CornerRadius="6"
                            Background="{ThemeResource SubtleFillColorSecondaryBrush}" VerticalAlignment="Top">
                        <FontIcon Glyph="&#xE71B;" FontSize="20" IsTextScaleFactorEnabled="False"
                                  Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                    </Border>
                    <StackPanel Grid.Column="1" Spacing="3">
                        <TextBlock Text="Connect to an existing gateway"
                                   Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock TextWrapping="Wrap"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Text="Already run an OpenClaw gateway — here, on another machine, or remote? Pair without installing anything." />
                    </StackPanel>
                    <FontIcon Grid.Column="2" Glyph="&#xE76C;" FontSize="14" IsTextScaleFactorEnabled="False"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                </Grid>
            </Button>

        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center" Margin="0,16,0,0"
                    IsTabStop="False" AutomationProperties.Name="Step 2 of 6">
            <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
            <Border Width="20" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupIndicatorAccentBrush}" />
            <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
            <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
            <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
            <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
        </StackPanel>
    </Grid>
</Page>
