<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClaw.App.Pages.Settings.GeneralSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="16" MaxWidth="600" HorizontalAlignment="Left">
            <TextBlock Text="General" Style="{StaticResource TitleTextBlockStyle}"/>

            <!-- Startup -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Startup" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <ToggleSwitch x:Name="AutoStartToggle" Header="Start with Windows"/>
                    <ToggleSwitch x:Name="HotkeyToggle" Header="Global hotkey (Ctrl+Alt+Shift+C)"/>
                </StackPanel>
            </Border>

            <!-- Appearance -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Appearance" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <ComboBox x:Name="ThemeCombo" Header="Theme" Width="200">
                        <ComboBoxItem Content="System default" Tag="Default"/>
                        <ComboBoxItem Content="Light" Tag="Light"/>
                        <ComboBoxItem Content="Dark" Tag="Dark"/>
                    </ComboBox>
                </StackPanel>
            </Border>

            <Button Content="Save" Style="{StaticResource AccentButtonStyle}" Click="OnSave" HorizontalAlignment="Right"/>
        </StackPanel>
    </ScrollViewer>
</Page>
