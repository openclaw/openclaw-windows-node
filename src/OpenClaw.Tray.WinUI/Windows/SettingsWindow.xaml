<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx
    x:Class="OpenClawTray.Windows.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:winex="using:WinUIEx"
    Title="Settings — OpenClaw Tray"
    MinWidth="400" MinHeight="500">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Scrollable content -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="24,24,24,12">
            <StackPanel Spacing="24" MaxWidth="450">
                
                <!-- Connection Section -->
                <StackPanel Spacing="8">
                    <TextBlock x:Uid="SettingsConnectionHeader" Text="CONNECTION" Style="{StaticResource CaptionTextBlockStyle}" 
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" FontWeight="Bold"/>

                    <Border AutomationProperties.AutomationId="SettingsTopologyGuide"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            CornerRadius="8"
                            Padding="12">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Gateway topology" FontWeight="SemiBold"/>
                            <TextBlock Text="Choose the setup that matches where your OpenClaw gateway runs. Local and WSL are fastest; SSH tunnel is safest for a remote Mac/Linux box; Tailscale/remote should use wss:// when available."
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       TextWrapping="Wrap"/>
                            <TextBlock x:Name="DetectedTopologyText"
                                       AutomationProperties.AutomationId="SettingsDetectedTopologyText"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       TextWrapping="Wrap"
                                       IsTextSelectionEnabled="True"/>
                            <Grid ColumnSpacing="8" RowSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <Button Grid.Row="0" Grid.Column="0"
                                        Content="Use Local Gateway"
                                        Click="OnUseLocalGateway"
                                        AutomationProperties.AutomationId="SettingsUseLocalGatewayButton"/>
                                <Button Grid.Row="0" Grid.Column="1"
                                        Content="Use WSL Gateway"
                                        Click="OnUseWslGateway"
                                        AutomationProperties.AutomationId="SettingsUseWslGatewayButton"/>
                                <Button Grid.Row="1" Grid.Column="0"
                                        Content="Use SSH Tunnel"
                                        Click="OnUseSshTunnel"
                                        AutomationProperties.AutomationId="SettingsUseSshTunnelButton"/>
                                <Button Grid.Row="1" Grid.Column="1"
                                        Content="Remote / Tailscale"
                                        Click="OnUseRemoteGateway"
                                        AutomationProperties.AutomationId="SettingsUseRemoteGatewayButton"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <ToggleSwitch x:Name="UseSshTunnelToggle" AutomationProperties.AutomationId="UseSshTunnelToggle" Header="Connect through SSH tunnel" Toggled="OnUseSshTunnelToggled"/>

                    <StackPanel x:Name="SshTunnelDetailsPanel" Spacing="8" Visibility="Collapsed">
                        <TextBlock Text="When enabled, the app will connect through ws://127.0.0.1:&lt;local-port&gt; and create an SSH forward to the remote gateway host."
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap"/>
                        <TextBlock AutomationProperties.AutomationId="SettingsSshBrowserForwardHint"
                                   Text="Browser proxy bridge: when enabled, the managed tunnel also forwards local-port+2 to remote-port+2 so browser.proxy can reach the browser control host."
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap"/>

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="SshTunnelUserTextBox" AutomationProperties.AutomationId="SshTunnelUserTextBox" Grid.Column="0" Header="SSH User" PlaceholderText="user" TextChanged="OnTopologyInputChanged"/>
                            <TextBox x:Name="SshTunnelHostTextBox" AutomationProperties.AutomationId="SshTunnelHostTextBox" Grid.Column="1" Header="SSH Host" PlaceholderText="machine-name or IP" TextChanged="OnTopologyInputChanged"/>
                        </Grid>

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="SshTunnelRemotePortTextBox" AutomationProperties.AutomationId="SshTunnelRemotePortTextBox" Grid.Column="0" Header="Remote Gateway Port" PlaceholderText="18789" TextChanged="OnTopologyInputChanged"/>
                            <TextBox x:Name="SshTunnelLocalPortTextBox" AutomationProperties.AutomationId="SshTunnelLocalPortTextBox" Grid.Column="1" Header="Local Forward Port" PlaceholderText="18789" TextChanged="OnSshTunnelLocalPortTextChanged"/>
                        </Grid>
                        <TextBlock x:Name="SshTunnelPreviewText"
                                   AutomationProperties.AutomationId="SettingsSshTunnelPreviewText"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap"
                                   IsTextSelectionEnabled="True"/>
                    </StackPanel>
                    
                    <TextBox x:Name="GatewayUrlTextBox" x:Uid="SettingsGatewayUrlTextBox" AutomationProperties.AutomationId="GatewayUrlTextBox" Header="Gateway URL" 
                             PlaceholderText="ws://localhost:18789 or https://host.tailnet.ts.net"
                             TextChanged="OnTopologyInputChanged"/>
                    
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBox x:Name="TokenTextBox" x:Uid="SettingsTokenTextBox" AutomationProperties.AutomationId="TokenTextBox" Header="Token" 
                                 PlaceholderText="Your API token" Width="300"/>
                        <Button x:Name="TestConnectionButton" x:Uid="SettingsTestConnectionButton" AutomationProperties.AutomationId="TestConnectionButton" Content="Test" 
                                VerticalAlignment="Bottom" Click="OnTestConnection"/>
                    </StackPanel>
                    
                    <TextBlock x:Name="StatusLabel" Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <Button x:Name="OpenCommandCenterButton"
                            AutomationProperties.AutomationId="SettingsOpenCommandCenterButton"
                            Content="Open Command Center"
                            Click="OnOpenCommandCenter"/>
                </StackPanel>

                <!-- Startup Section -->
                <StackPanel Spacing="8">
                    <TextBlock x:Uid="SettingsStartupHeader" Text="STARTUP" Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" FontWeight="Bold"/>
                    
                    <ToggleSwitch x:Name="AutoStartToggle" x:Uid="SettingsAutoStartToggle" AutomationProperties.AutomationId="AutoStartToggle" Header="Start automatically with Windows"/>
                    <ToggleSwitch x:Name="GlobalHotkeyToggle" x:Uid="SettingsGlobalHotkeyToggle" AutomationProperties.AutomationId="GlobalHotkeyToggle" Header="Global hotkey (Ctrl+Alt+Shift+C → Quick Send)"/>
                </StackPanel>

                <!-- Notifications Section -->
                <StackPanel Spacing="8">
                    <TextBlock x:Uid="SettingsNotificationsHeader" Text="NOTIFICATIONS" Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" FontWeight="Bold"/>
                    
                    <ToggleSwitch x:Name="NotificationsToggle" x:Uid="SettingsNotificationsToggle" AutomationProperties.AutomationId="NotificationsToggle" Header="Show notifications"/>
                    
                    <ComboBox x:Name="NotificationSoundComboBox" x:Uid="SettingsSoundComboBox" AutomationProperties.AutomationId="NotificationSoundComboBox" Header="Sound" Width="200">
                        <ComboBoxItem x:Uid="SettingsSoundDefault" Content="Default" Tag="Default"/>
                        <ComboBoxItem x:Uid="SettingsSoundNone" Content="None" Tag="None"/>
                        <ComboBoxItem x:Uid="SettingsSoundSubtle" Content="Subtle" Tag="Subtle"/>
                    </ComboBox>
                    
                    <TextBlock x:Uid="SettingsNotifyForLabel" Text="Show notifications for:" Margin="0,8,0,0"/>
                    <TextBlock x:Uid="SettingsNotifyFilterHint" Text="Filters by keywords in the message (e.g., 'email', 'reminder')" 
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Margin="0,0,0,4"/>
                    
                    <StackPanel Spacing="4">
                        <CheckBox x:Name="NotifyHealthCb" x:Uid="SettingsNotifyHealthCb" AutomationProperties.AutomationId="NotifyHealthCb" Content="Health alerts"/>
                        <CheckBox x:Name="NotifyUrgentCb" x:Uid="SettingsNotifyUrgentCb" AutomationProperties.AutomationId="NotifyUrgentCb" Content="Urgent messages"/>
                        <CheckBox x:Name="NotifyReminderCb" x:Uid="SettingsNotifyReminderCb" AutomationProperties.AutomationId="NotifyReminderCb" Content="Reminders"/>
                        <CheckBox x:Name="NotifyEmailCb" x:Uid="SettingsNotifyEmailCb" AutomationProperties.AutomationId="NotifyEmailCb" Content="Email summaries"/>
                        <CheckBox x:Name="NotifyCalendarCb" x:Uid="SettingsNotifyCalendarCb" AutomationProperties.AutomationId="NotifyCalendarCb" Content="Calendar events"/>
                        <CheckBox x:Name="NotifyBuildCb" x:Uid="SettingsNotifyBuildCb" AutomationProperties.AutomationId="NotifyBuildCb" Content="Build notifications"/>
                        <CheckBox x:Name="NotifyStockCb" x:Uid="SettingsNotifyStockCb" AutomationProperties.AutomationId="NotifyStockCb" Content="Stock alerts"/>
                        <CheckBox x:Name="NotifyInfoCb" x:Uid="SettingsNotifyInfoCb" AutomationProperties.AutomationId="NotifyInfoCb" Content="Info messages"/>
                    </StackPanel>
                </StackPanel>

                <!-- Test Notification -->
                <Button x:Name="TestNotificationButton" x:Uid="SettingsTestNotificationButton" AutomationProperties.AutomationId="TestNotificationButton" Content="Send Test Notification" 
                        Click="OnTestNotification"/>
                
                <!-- Advanced Section -->
                <StackPanel Spacing="8">
                    <TextBlock x:Uid="SettingsAdvancedHeader" Text="ADVANCED (EXPERIMENTAL)" Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" FontWeight="Bold"/>
                    
                    <ToggleSwitch x:Name="NodeModeToggle" x:Uid="SettingsNodeModeToggle" AutomationProperties.AutomationId="NodeModeToggle" Header="Enable Node Mode"/>
                    <TextBlock x:Uid="SettingsNodeModeDescription" Text="When enabled, this PC can receive commands from the agent (canvas, screenshots, etc.)"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextWrapping="Wrap"
                               Margin="0,-4,0,0"/>
                    <Border AutomationProperties.AutomationId="SettingsNodeCapabilityToggles"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            CornerRadius="8"
                            Padding="12">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Node capability groups" FontWeight="SemiBold"/>
                            <TextBlock Text="Disable capabilities you do not want this Windows node to advertise. System notifications, safe command execution with approvals, and device status remain available while Node Mode is on."
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       TextWrapping="Wrap"/>
                            <ToggleSwitch x:Name="NodeCanvasToggle" AutomationProperties.AutomationId="NodeCanvasToggle" Header="Canvas / WebView2"/>
                            <ToggleSwitch x:Name="NodeScreenToggle" AutomationProperties.AutomationId="NodeScreenToggle" Header="Screen snapshot and recording"/>
                            <ToggleSwitch x:Name="NodeCameraToggle" AutomationProperties.AutomationId="NodeCameraToggle" Header="Camera list, snapshot, and clips"/>
                            <ToggleSwitch x:Name="NodeLocationToggle" AutomationProperties.AutomationId="NodeLocationToggle" Header="Location"/>
                            <ToggleSwitch x:Name="NodeBrowserProxyToggle" AutomationProperties.AutomationId="NodeBrowserProxyToggle" Header="Browser proxy bridge" Toggled="OnNodeBrowserProxyToggled"/>
                        </StackPanel>
                    </Border>

                    <TextBlock x:Uid="SettingsDeveloperModeHeader" Text="DEVELOPER MODE" Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" FontWeight="Bold" Margin="0,16,0,0"/>
                    <ToggleSwitch x:Name="McpServerToggle" x:Uid="SettingsMcpServerToggle" AutomationProperties.AutomationId="McpServerToggle" Header="Enable Local MCP Server" Margin="0,8,0,0"/>
                    <TextBlock x:Uid="SettingsMcpDescription"
                               Text="Exposes the same node capabilities (system, screen, camera, canvas) to local MCP clients (Claude Desktop, Cursor, Claude Code)."
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextWrapping="Wrap"
                               Margin="0,-4,0,0"/>
                    <Grid Margin="0,4,0,0" ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" x:Uid="SettingsMcpEndpointLabel" Text="Endpoint:" Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBox Grid.Column="1" x:Name="McpUrlTextBox" IsReadOnly="True"
                                 BorderThickness="0" Background="Transparent" Padding="0"
                                 FontFamily="Consolas" FontSize="12"/>
                    </Grid>
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" x:Uid="SettingsMcpStatusLabel" Text="Status:" Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Grid.Column="1" x:Name="McpStatusText"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </Grid>
                    <Grid Margin="0,4,0,0" ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                             <ColumnDefinition Width="*"/>
                             <ColumnDefinition Width="Auto"/>
                             <ColumnDefinition Width="Auto"/>
                             <ColumnDefinition Width="Auto"/>
                         </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" x:Uid="SettingsMcpTokenLabel" Text="Bearer token:" Style="{StaticResource CaptionTextBlockStyle}"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBox Grid.Column="1" x:Name="McpTokenTextBox" IsReadOnly="True"
                                 BorderThickness="0" Background="Transparent" Padding="0"
                                 FontFamily="Consolas" FontSize="12"
                                 VerticalAlignment="Center"
                                 AutomationProperties.AutomationId="McpTokenTextBox"/>
                        <Button Grid.Column="2" x:Name="McpTokenRevealButton" x:Uid="SettingsMcpTokenRevealButton"
                                AutomationProperties.AutomationId="McpTokenRevealButton"
                                Content="Reveal" Click="OnRevealMcpToken" Padding="8,2"/>
                         <Button Grid.Column="3" x:Name="McpTokenCopyButton" x:Uid="SettingsMcpTokenCopyButton"
                                 AutomationProperties.AutomationId="McpTokenCopyButton"
                                 Content="Copy" Click="OnCopyMcpToken" Padding="8,2" Margin="4,0,0,0"/>
                         <Button Grid.Column="4" x:Name="McpTokenResetButton" x:Uid="SettingsMcpTokenResetButton"
                                 AutomationProperties.AutomationId="McpTokenResetButton"
                                 Content="Reset" Click="OnResetMcpToken" Padding="8,2" Margin="4,0,0,0"/>
                     </Grid>
                    <TextBlock x:Uid="SettingsMcpTokenHint"
                               x:Name="McpTokenHintText"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextWrapping="Wrap"
                               Margin="0,2,0,0"/>
                </StackPanel>
                
            </StackPanel>
        </ScrollViewer>
        
        <!-- Fixed footer with buttons -->
        <Border Grid.Row="1" 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                Padding="24,16">
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                <Button x:Uid="SettingsCancelButton" Content="Cancel" Click="OnCancel" Width="80"/>
                <Button x:Name="SaveButton" x:Uid="SettingsSaveButton" AutomationProperties.AutomationId="SaveButton" Content="Save" 
                        Click="OnSave" Width="80" Style="{ThemeResource AccentButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</winex:WindowEx>

