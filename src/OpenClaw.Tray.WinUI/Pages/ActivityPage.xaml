<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClawTray.Pages.ActivityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
            <TextBlock x:Uid="ActivityPage_TextBlock_17" Text="Activity" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock x:Name="CountText" Text="(0)" VerticalAlignment="Center"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Filter bar -->
        <StackPanel x:Name="FilterPanel" Grid.Row="1" Orientation="Horizontal" Spacing="4" Margin="0,0,0,16">
            <ToggleButton x:Uid="FilterAllButton" x:Name="FilterAllButton" Content="All" Tag="all" IsChecked="True"
                          Click="OnFilterClick"
                          AutomationProperties.AutomationId="FilterAllButton"/>
            <ToggleButton x:Uid="FilterSessionsButton" x:Name="FilterSessionsButton" Content="Sessions" Tag="session"
                          Click="OnFilterClick"
                          AutomationProperties.AutomationId="FilterSessionsButton"/>
            <ToggleButton x:Uid="FilterUsageButton" x:Name="FilterUsageButton" Content="Usage" Tag="usage"
                          Click="OnFilterClick"
                          AutomationProperties.AutomationId="FilterUsageButton"/>
            <ToggleButton x:Uid="FilterNodesButton" x:Name="FilterNodesButton" Content="Nodes" Tag="node"
                          Click="OnFilterClick"
                          AutomationProperties.AutomationId="FilterNodesButton"/>
            <ToggleButton x:Uid="FilterNotificationsButton" x:Name="FilterNotificationsButton" Content="Notifications" Tag="notification"
                          Click="OnFilterClick"
                          AutomationProperties.AutomationId="FilterNotificationsButton"/>
        </StackPanel>

        <!-- Activity list -->
        <ListView x:Name="ActivityList" Grid.Row="2"
                  AutomationProperties.AutomationId="ActivityList"
                  SelectionMode="None"
                  Visibility="Collapsed">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12" Margin="0,2" CornerRadius="4"
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Title}" FontWeight="SemiBold"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TimeAgo}"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                        <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding DetailText}"
                                   TextWrapping="Wrap" MaxLines="2"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Visibility="{Binding DetailVisibility}"/>

                        <Border Grid.Row="2" Grid.ColumnSpan="2"
                                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                CornerRadius="2" Padding="4,2" Margin="0,4,0,0"
                                HorizontalAlignment="Left">
                            <TextBlock Text="{Binding Category}"
                                       Style="{StaticResource CaptionTextBlockStyle}"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel x:Name="EmptyState" Grid.Row="2"
                    VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8">
            <TextBlock Text="🧭" FontSize="48" HorizontalAlignment="Center"/>
            <TextBlock x:Uid="ActivityPage_TextBlock_86" Text="No activity yet"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       HorizontalAlignment="Center"/>
            <TextBlock x:Uid="ActivityPage_TextBlock_89" Text="Activity from sessions, notifications, and node events will appear here."
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center" TextWrapping="Wrap" MaxWidth="320"/>
        </StackPanel>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Uid="ClearAllButton" x:Name="ClearAllButton" Content="Clear All" Click="OnClearAll"
                    AutomationProperties.AutomationId="ClearAllButton"/>
        </StackPanel>
    </Grid>
</Page>
