<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClawTray.Pages.ConversationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Enabled">

    <Grid Padding="24,16,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock x:Uid="ConversationsPage_TextBlock_16" Grid.Row="0" Text="Conversations"
                   Style="{StaticResource TitleTextBlockStyle}"/>

        <!-- Toolbar: filter + refresh -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="FilterCombo" Grid.Column="0"
                      MinWidth="160" SelectionChanged="FilterCombo_SelectionChanged">
                <ComboBoxItem x:Uid="ConversationsPage_ComboBoxItem_29" Content="All" Tag="all" IsSelected="True"/>
                <ComboBoxItem x:Uid="ConversationsPage_ComboBoxItem_30" Content="By Channel" Tag="channel"/>
                <ComboBoxItem x:Uid="ConversationsPage_ComboBoxItem_31" Content="By Status" Tag="status"/>
            </ComboBox>

            <Button Grid.Column="2" Click="OnRefresh">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                    <TextBlock x:Uid="ConversationsPage_TextBlock_37" Text="Refresh"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Not-connected warning -->
        <InfoBar x:Uid="ConnectionWarning" x:Name="ConnectionWarning" Grid.Row="2"
                 Title="Not connected"
                 Message="Connect to a gateway to view conversations."
                 Severity="Warning" IsOpen="False" IsClosable="False"
                 VerticalAlignment="Top"/>

        <!-- Empty state -->
        <TextBlock x:Uid="EmptyState" x:Name="EmptyState" Grid.Row="2"
                   Text="No sessions found"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Visibility="Collapsed"/>

        <!-- Session list -->
        <ListView x:Name="SessionListView" Grid.Row="2"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8,10" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Status dot -->
                        <Ellipse Grid.Column="0" Grid.RowSpan="2"
                                 Width="10" Height="10" VerticalAlignment="Center"
                                 Fill="{Binding StatusColor}"/>

                        <!-- Row 1: Key + time -->
                        <TextBlock Grid.Column="1" Grid.Row="0"
                                   Text="{Binding Key}" FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="2" Grid.Row="0"
                                   Text="{Binding AgeText}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   FontSize="12"/>

                        <!-- Row 2: details -->
                        <TextBlock Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2"
                                   Text="{Binding Details}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   FontSize="12" TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
