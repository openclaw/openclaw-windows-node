<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClawTray.Pages.BindingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24" Spacing="16" HorizontalAlignment="Stretch">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="4">
                    <TextBlock x:Uid="BindingsPage_TextBlock_16" Text="🔀 Bindings" Style="{StaticResource TitleTextBlockStyle}"/>
                    <TextBlock x:Uid="BindingsPage_TextBlock_17" Text="Message routing rules"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Style="{StaticResource CaptionTextBlockStyle}"/>
                </StackPanel>
                <Button x:Name="RefreshButton" Grid.Column="1" Click="RefreshButton_Click"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                        <TextBlock x:Uid="BindingsPage_TextBlock_25" Text="Refresh"/>
                    </StackPanel>
                </Button>
            </Grid>

            <InfoBar x:Uid="PriorityInfoBar" x:Name="PriorityInfoBar"
                     IsOpen="True" IsClosable="True"
                     Severity="Informational"
                     Title="Routing priority"
                     Message="Bindings control which agent handles messages from each channel. Priority: peer > account > channel > default."/>

            <InfoBar x:Uid="SingleAgentInfoBar" x:Name="SingleAgentInfoBar"
                     IsOpen="False" IsClosable="False"
                     Severity="Informational"
                     Title="Single-agent mode"
                     Message="All messages route to the default agent. Add bindings for multi-agent routing."/>

            <ListView x:Name="BindingsList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Channel icon + name -->
                                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center" Margin="0,0,16,0">
                                    <TextBlock Text="{Binding ChannelIcon}" FontSize="18"/>
                                    <TextBlock Text="{Binding Channel}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </StackPanel>

                                <!-- Account + Peer -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                    <TextBlock Text="{Binding AccountId}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               FontSize="12"/>
                                    <TextBlock Text="{Binding PeerDisplay}"
                                               FontSize="12"/>
                                </StackPanel>

                                <!-- Arrow -->
                                <TextBlock Grid.Column="3" Text="→" FontSize="18"
                                           VerticalAlignment="Center" Margin="12,0"/>

                                <!-- Agent -->
                                <TextBlock Grid.Column="4" Text="{Binding AgentId}"
                                           FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackPanel>
    </ScrollViewer>
</Page>
